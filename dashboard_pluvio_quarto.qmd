---
title: "Tableau de bord pluviométrique"
execute: 
  echo: false
format:
  dashboard:
    theme: cosmo
    orientation: rows
    include-after-body: fonctions_js.html
    embed-resources: true
    nav-buttons:
      - icon: github
        href: https://github.com/marinemarjou/pluviometrie-kermorvan
---

```{r setup}
library(dplyr)
library(tidyr)
library(ggplot2)
library(crosstalk)
library(plotly)
library(htmltools)

pluie <- read.csv("pluie.csv",sep=";", header=TRUE)

pluie <- pluie %>%
  gather("mois", "mm", 2:13)

pluie$mm <- as.numeric(pluie$mm)
pluie$annee <- as.factor(pluie$annee)
pluie$mois <- factor(pluie$mois, levels = c("janvier", "fevrier", "mars",
                                            "avril", "mai", "juin", "juillet",
                                            "aout", "septembre", "octobre", 
                                            "novembre", "decembre"))

```

# Accueil {.tabset}

```{r}
#| include: false

pluie_obs <- pluie |>
  mutate(annee = as.integer(as.character(annee)))

annee_en_cours = max(pluie_obs$annee)

cumul_annuel <- pluie_obs |>
  group_by(annee) |>
  summarise(cumul = sum(mm, na.rm = TRUE), .groups = "drop")

periode_suivi <- paste0(min(cumul_annuel$annee), "–", max(cumul_annuel$annee))

annee_seche <- cumul_annuel |> filter(annee != annee_en_cours) |> slice_min(cumul, n = 1)
annee_pluvieuse <- cumul_annuel |> slice_max(cumul, n = 1)

annee_en_cours <- max(cumul_annuel$annee)

cumul_en_cours <- cumul_annuel |>
  filter(annee == annee_en_cours)
```

## Row


**Bienvenue sur le tableau de bord pluviométrique de Daniel !**

Depuis 2007, un petit pluviomètre est consulté chaque jour, ou presque, par Daniel, ancien agriculteur et jardinier amateur à ses heures perdues, avec une régularité qui ferait pâlir bien des stations météorologiques. Relevé après relevé, il a permis de constituer une base de données unique retraçant près de vingt ans de pluie bretonne au lieu-dit Le Gouret, anciennement Kermorvan pour les locaux.

Ici, pas de modèles climatiques à plusieurs millions d'euros, pas de satellites ni de supercalculateurs… juste un pluviomètre, des calendriers et beaucoup de persévérance. Une approche artisanale qui n'a rien à envier aux grandes institutions...

N'hésitez pas à parcourir l'ensemble des onglets à votre disposition. Les graphiques sont interactifs et peuvent être visualisés en grand écran en cliquant sur le bouton en bas à droite de l'encart.


## Row 

```{r}
#| content: valuebox
list(
  icon = "calendar",
  title = "Période suivie",
  value = periode_suivi,
  color = "info"
)
```

```{r}
#| content: valuebox
list(
  icon = "sun",
  title = "Année la plus sèche",
  value = paste0(annee_seche$annee, " — ", annee_seche$cumul, " mm"),
  color = "warning"
)
```

```{r}
#| content: valuebox
list(
  icon = "cloud-rain",
  title = "Année la plus pluvieuse",
  value = paste0(annee_pluvieuse$annee, " — ", annee_pluvieuse$cumul, " mm"),
  color = "primary"
)
```

```{r}
#| content: valuebox
list(
  icon = "droplet",
  title = paste0("Cumul ", annee_en_cours),
  value = paste0(cumul_en_cours$cumul, " mm"),
  color = "success"
)
```

## Row

**Crédits :**

- Collecte des données : Daniel
- Création et maintenance de l'application : Marine



# Synthèse annuelle {.tabset}

## Choix : {.tabset}

```{r}
#| title: "Vue mensuelle"

shared_pluie <- highlight_key(pluie)

# shared_pluie <- SharedData$new(pluie)

filtre_annee <- filter_select(
  id = "filtre_annee",
  label = "Année",
  sharedData = shared_pluie,
  group = ~annee,
  multiple = FALSE
)


p <- plot_ly(
  data = shared_pluie,
  x = ~mois,
  y = ~mm,
  type = "bar",
  marker = list(color = ~mm)
)


bscols(
  widths = c(2, 9),
  filtre_annee,
  p
  
)
```


```{r}
#| title: "Vue d'ensemble"
gg_annee <- ggplot(pluie) +
  aes(x = mois, y = mm, fill=mm, 
      text= paste0(mois, " ", annee, "<br>",mm," mm")) +
  scale_fill_gradient(high = "#132B43", low = "#56B1F7")+
  geom_col() +
  # scale_y_continuous(breaks=seq(0,220,25), limits = c(0,225)) +
  labs(x="", y="") +
  facet_wrap(~annee) +
  theme_bw() + 
  theme(legend.position='none',
        axis.text.x = element_text(angle = 40, hjust=1, size = 8))

plotly::ggplotly(gg_annee, tooltip = "text")
```


```{r}
#| title: 'Cumul annuel'
cumul <- pluie %>%
  group_by(annee) %>%
  summarise(tot=sum(mm,na.rm=TRUE))


gg_cumul <- 
  ggplot(cumul) +
  aes(x = annee, y = tot, fill = tot) + 
  scale_fill_gradient(high = "#132B43", low = "#56B1F7")+
  geom_col() +
  labs(x="", y="") +
  theme_bw() + theme(legend.position='none') 

ggplotly(gg_cumul,
         tooltip = "tot")
```

# Visualisation mensuelle {.tabset}

```{r}

pluie_annees <- pluie 

mois_dispo <- unique(pluie_annees$mois)

p <- plot_ly()

for (i in seq_along(mois_dispo)) {
  mois_i <- mois_dispo[i]
  
  data_i <- pluie_annees |>
    filter(mois == mois_i)
  
  med_i <- round(median(data_i$mm, na.rm = TRUE),0)
  moy_i <- round(mean(data_i$mm, na.rm = TRUE),0)
  
  p <- p |>
    add_bars(
      data = data_i,
      x = ~annee,
      y = ~mm,
      name = "",
      marker = list(color = data_i$mm),
      visible = i == 1,
      showlegend = FALSE
    ) |>
    add_lines(
      data = data_i,
      x = ~annee,
      y = rep(med_i, nrow(data_i)),
      name = paste0("Mediane : ", round(med_i, 1), " mm"),
      visible = i == 1,
      line = list(width = 3, color = "black"),
      showlegend = TRUE
    ) |> 
    add_lines(
      data = data_i,
      x = ~annee,
      y = rep(moy_i, nrow(data_i)),
      name = paste0("Moyenne : ", round(moy_i, 1), " mm"),
      line = list(color = "black", width = 3, dash = "dash"),
      visible = i == 1,
      showlegend = TRUE
    )
}

boutons <- lapply(seq_along(mois_dispo), function(i) {
  visibles <- rep(FALSE, length(mois_dispo) * 3)
  visibles[(3 * i - 2):(3 * i)] <- TRUE
  
  list(
    method = "update",
    label = mois_dispo[i],
    args = list(
      list(visible = visibles),
      list(title = paste("Pluviométrie mensuelle -", mois_dispo[i]))
    )
  )
})

p |>
  layout(
    title = paste("Pluviométrie mensuelle -", mois_dispo[1]),
    xaxis = list(title = "Année"),
    yaxis = list(title = "Pluie (mm)"),
  updatemenus = list(
    list(
      x = 0,
      y = 1.18,
      xanchor = "left",
      yanchor = "top",
      buttons = boutons
    )
  )
  )

```


# À suivre... {.tabset}

Lorsque la motivation, le temps et la créativité seront de nouveaux rassemblés pourquoi ne pas proposer de nouveaux onglets. Toutes les propositions sont les bienvenues.